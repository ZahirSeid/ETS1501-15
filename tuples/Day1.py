tuples_example = ('banana', 'apples', 'orange', 'banana')

print(tuples_example.count('banana')) # this will count how many occurence does the sting have in the tuple

print(tuples_example.index('orange')) # the index method will allow us to identify the index of the string object 

print(tuples_example.index('banana')) # this will only print out for us the first occurence of the string in the tuple 

# output

"""
2
2
0
"""
